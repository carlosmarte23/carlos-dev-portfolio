---
import Container from "@components/ui/Container.astro";
import NavLink from "@components/layout/NavLink.astro";
import BrandLogo from "@components/layout/BrandLogo.astro";
import ThemeToggle from "./ThemeToggle.astro";
import MobileMenu from "./MobileMenu.astro";
import { site } from "@data/site";
import { navigation } from "@data/navigation";

const navItems = navigation.main;
---

<header class="sticky top-0 z-50 bg-app/90 backdrop-blur">
  <Container class="px-4">
    <div class="flex h-(--header-height) items-center justify-between gap-6">
      <a
        href="/"
        class="inline-flex items-center transition hover:scale-105 focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-accent"
        aria-label={`${site.name} home`}
        title={site.name}
      >
        <BrandLogo class="text-lg" />
      </a>

      <nav
        class="hidden items-center gap-8 md:flex"
        aria-label="Main navigation"
      >
        {
          navItems.map((item) => (
            <NavLink href={item.href} label={item.label} />
          ))
        }
      </nav>

      <MobileMenu navItems={navItems} />

      <div class="hidden items-center justify-between md:flex">
        <ThemeToggle />
      </div>
    </div>
  </Container>
</header>
