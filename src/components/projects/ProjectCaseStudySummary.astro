---
import type {
  ProjectCaseStudy,
  ProjectDetailContent,
} from "@content-types/content";
import Card from "@/components/ui/Card.astro";
import { BadgeCheck, CircleAlert, PanelsTopLeft } from "@lucide/astro";

interface Props {
  caseStudy: ProjectCaseStudy;
  content: ProjectDetailContent["caseStudySummary"];
}

const { caseStudy, content } = Astro.props;
---

<section class="grid grid-cols-1 gap-5 md:grid-cols-2 lg:grid-cols-3">
  <Card
    padding="sm"
    class="timeline-view animate-slide-up-fade space-y-4 animate-range-[entry_0%_contain_20%] animate-duration-slow animate-bezier-expo-out"
  >
    <div class="flex items-center gap-3">
      <PanelsTopLeft class="size-8 text-accent" aria-hidden="true" />
      <h3 class="font-display text-xl font-bold text-main">
        {content.overviewTitle}
      </h3>
    </div>
    <p class="text-muted">{caseStudy.overview}</p>
  </Card>

  <Card
    padding="sm"
    class="timeline-view animate-slide-up-fade space-y-4 animate-delay-100 animate-range-[entry_0%_contain_20%] animate-duration-slow animate-bezier-expo-out"
  >
    <div class="flex items-center gap-3">
      <CircleAlert class="size-8 text-accent" aria-hidden="true" />
      <h3 class="font-display text-xl font-bold text-main">
        {content.problemTitle}
      </h3>
    </div>
    <p class="text-muted">{caseStudy.problem}</p>
  </Card>

  <Card
    padding="sm"
    class="timeline-view animate-slide-up-fade space-y-4 animate-delay-200 animate-range-[entry_0%_contain_20%] animate-duration-slow animate-bezier-expo-out"
  >
    <div class="flex items-center gap-3">
      <BadgeCheck class="size-8 text-accent" aria-hidden="true" />
      <h3 class="font-display text-xl font-bold text-main">
        {content.solutionTitle}
      </h3>
    </div>
    <p class="text-muted">{caseStudy.solution}</p>
  </Card>
</section>
